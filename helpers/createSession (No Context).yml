info:
  name: createSession (No Context)
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/sessions/init"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
