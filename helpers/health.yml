info:
  name: health
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
