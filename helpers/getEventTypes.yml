info:
  name: getEventTypes
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/events/types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
