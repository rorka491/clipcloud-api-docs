info:
  name: ws
  type: websocket
  seq: 2

websocket:
  url: "{{WS_BASE_URL}}"
  headers:
    - name: Cookie
      value: access_token={{access_token}}
  message:
    - title: message 1
      selected: false
      message:
        type: json
        data: ""
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
