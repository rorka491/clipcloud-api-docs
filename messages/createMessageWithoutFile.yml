info:
  name: createMessageWithoutFile
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/messages"
  headers:
    - name: Content-Type
      value: application/json
    - name: X-Session-id
      value: "{{session_id}}"
  body:
    type: json
    data: |-
      {
        "text": "Мамуля"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        bru.setEnvVar("message_id", body.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
