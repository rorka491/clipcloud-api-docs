info:
  name: searchMessage
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/messages/search"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
