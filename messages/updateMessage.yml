info:
  name: updateMessage
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE_URL}}/messages/{{message_id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: X-Session-id
      value: "{{session_id}}"
  body:
    type: json
    data: |-
      {
        "text": "test"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
