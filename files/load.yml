info:
  name: load
  type: http
  seq: 3

http:
  method: GET
  url: "{{load_file_url}}"
  body:
    type: file
    data:
      - filePath: ""
        contentType: ""
        selected: true

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
