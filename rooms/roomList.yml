info:
  name: roomList
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE_URL}}/rooms"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
