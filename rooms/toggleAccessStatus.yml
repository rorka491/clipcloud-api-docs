info:
  name: toggleAccessStatus
  type: http
  seq: 9

http:
  method: POST
  url: "{{BASE_URL}}/rooms/toggle-access-status"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
