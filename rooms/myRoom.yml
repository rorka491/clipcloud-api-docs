info:
  name: myRoom
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/rooms/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
