info:
  name: roomHistory
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/rooms/history"
  headers:
    - name: X-Session-id
      value: "{{session_id}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
