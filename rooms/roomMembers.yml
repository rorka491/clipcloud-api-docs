info:
  name: roomMembers
  type: http
  seq: 7

http:
  method: POST
  url: "{{BASE_URL}}/rooms"
  headers:
    - name: X-Session-id
      value: "{{session_id}}"
      disabled: true

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        bru.setEnvVar("room_code", body.room_code);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
