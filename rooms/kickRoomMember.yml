info:
  name: kickRoomMember
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URL}}/rooms/members/kick?session-id="
  headers:
    - name: X-Session-id
      value: "{{session_id}}"
      disabled: true
  params:
    - name: session-id
      value: ""
      type: query

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        bru.setEnvVar("room_code", body.room_code);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
