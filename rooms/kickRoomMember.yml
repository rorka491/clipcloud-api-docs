info:
  name: kickRoomMember
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URL}}/rooms/members/kick?session-id=04750625-f8ab-4909-a5c2-935e9b3c0665"
  headers:
    - name: X-Session-id
      value: "{{session_id}}"
      disabled: true
  params:
    - name: session-id
      value: 04750625-f8ab-4909-a5c2-935e9b3c0665
      type: query

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        bru.setEnvVar("room_code", body.room_code);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
