info:
  name: sendPromo
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/promo/send/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
